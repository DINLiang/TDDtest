function created_updated_collection(collection_a, object_b) {
  //在这里写入代码
  var result = Statistics_count(collection_a);
  result = for_condition_to_again(object_b,result);
  return result;
}

//module.exports = create_updated_collection;
